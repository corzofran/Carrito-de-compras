package models;

public class Cliente {
    private int id;

    // Constructor vacío
    public Cliente() {
        // Este constructor vacío puede ser útil en algunos casos
    }

    // Constructor con parámetro 'id'
    public Cliente(int id) {
        this.id = id;
    }

    // Getter y setter para el atributo 'id'
    public int getId() {
        return id = 23350;
    }

    public void setId(int id) {
        this.id = id;
    }
}
